#include<stdio.h>
main()
	int sum=0,diff=0,a,b,c,d;
	printf("enter the nos to be addeed:");
	scanf("%d%d",&a,&b);
	sum=a+b;
	printf("The sum of to nos is: ",sum);
	printf("enter th nos to be substract:");
	scanf("%d%d" ,&c,&d);
	diff=c-d;
	printf("diff of two nos is: ",diff);

